# Native macOS host (Swift)

`HumanizerApp.swift` is the menu-bar / Login Items host compiled into `Humanizer.app`.
`LaunchAtLogin.swift` is the helper under `Contents/Library/LoginItems`.

Built by `scripts/build_macos_app.sh`.
